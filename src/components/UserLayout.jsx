import React from 'react'
import { Outlet } from 'react-router'

const UserLayout = () => {
    return (
        <>
            <div>❤</div>
            <Outlet />
        </>
    )
}

export default UserLayout